spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/gr1993/ecommerce-msa-config

server:
  port: 8888

encrypt:
  key: ${CONFIG_KEY:myConfigKey}