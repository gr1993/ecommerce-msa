spring:
  # Config Server 비활성화 (테스트 환경)
  cloud:
    config:
      enabled: false
  config:
    import: ""

springwolf:
  enabled: false  # 전체 기능 비활성화
  scanner:
    async-api-json:
      enabled: false